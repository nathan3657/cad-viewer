# 🤝 CAD Viewer 项目交接文档 (HANDOFF.md)

本文件是为**新开辟的 AI 助手会话**或**接替的开发人员**编写的完整技术上下文交接。在接手后，通过阅读此文档即可无缝理解项目现状，迅速进入开发状态。

---

## 🎯 1. 我们在做什么任务？ (Project Goal)
本项目目标是基于 `pnpm monorepo` 架构的 **mlightcad/cad-viewer** 建立一个**完全适用于纯内网、物理隔离环境的极速 CAD 全栈查看器与活码分享系统**。
核心需求为：
1. **纯本地决策查看与上传分享双轨交互**：用户拖入 DWG/DXF 图纸后，自主选择：
   * **直接打开**：免流量纯本地极速解析（无需上传后端，纯前端 Canvas + Three.js 绘制）。
   * **上传并生成二维码**：上传至服务器，自动在下方生成一个二维码用于局域网内其他设备扫码查看。
2. **二维码自动保存与加水印**：前端在生成分享二维码的瞬间，全自动合成“带有文件名文字”的水印二维码并下载，同时**静默备份**发送给后端，保存于服务器的 `public/drawings/qrcodes/` 目录中。
3. **全套字体的本地离线自托管**：在没有互联网的内网环境下，所有中英文字体全部拉取本地物理字库，并设计**完美中文缺字兜底机制**，解决图纸文字空白或问号问题。
4. **零依赖极简单文件部署**：前端代码打包并剔除大文件，后端利用 `esbuild` 打包成单个 `server.cjs`（约 1.1MB），只需 Node.js 环境即可一键启动服务。

---

## ✅ 2. 已经完成了什么？ (Accomplishments)
1. **交互逻辑全部重构并回滚至纯净版**：
   * 彻底剥离了早期引入的 ODAFileConverter 后端依赖（回退到轻量级的前端 DXF 解析）。
   * 前端实现“选图纸 ➡️ 弹出决策按钮（直接查看/上传分享）”的清晰逻辑。
2. **后端单文件服务开发完成**：
   * 编写了 `packages/cad-viewer-example/server.js`，包含大文件二进制流式上传接口 `/api/upload`、带水印二维码静默备份接口 `/api/save-qrcode`，以及对静态目录和数据目录的分区托管。
3. **离线常用字体抓取与就绪**：
   * 通过 Node 离线下载工具抓取了包括 `hztxt.shx`（经典中文兜底）、`txt.shx`、`gbenor.shx` 等 8 款最核心中英文字体，物理存放在开发目录 `packages/cad-viewer-example/public/drawings/fonts/` 下。
4. **完成了 100% 绝对写保护的部署打包管道**：
   * 修正并优化了 `package.json` 中的 `build:release` 编译脚本。日常升级编译时，会自动隔离 `public/` 物理资产盘，**绝对不会重写或覆盖内网服务器上已存在的图纸、二维码和海量字体资产**。
5. **Git 分支整理归档**：
   * 目前本地与远程个人 GitHub 仓库对应的分支已全部重命名对齐为 **`QRcode`**。

---

## 📍 3. 当前进展到哪？ (Current Status)
* **最新完美版代码已全部构建成功，且源码与文档已被自动提交并推送至 GitHub 个人仓库的 `QRcode` 分支**。
* 已经在局域网的 Windows/Mac 上通过了全流程验证，文字绘制完备，二维码活码跳转与防缓存机制运作极其顺畅。

---

## 💡 4. 核心踩坑与技术避雷针 (Lessons Learned & Crucial Pitfalls)
在续写或修改本项目的代码时，**必须死死记住以下两项极其关键的 Web 基础与底层渲染机制**，否则极易引发 404 崩溃或文字空白：

### ⚠️ 避雷针 A：URL 哈希锚点截断与二次 URL 编码机制
* **痛点**：当上传的图纸文件名包含 **`#`**、空格、`&` 等在 URL 中具有特殊语义的字符（例如 `104 送水泵站5#~7#控制图.dwg`）时，**浏览器在发送 HTTP 物理请求时，会在 `#` 的位置直接截断**，只向服务器请求 `/drawings/uploads/104 送水泵站5`，从而发生 404 报错。
* **二次解码机制**：当前端从浏览器地址栏通过 `urlParams.get('drawing')` 提取参数时，浏览器会**自动执行一次 URL 解码**，将安全的 `%23` 字符重新变回明文的 `#`。如果直接把此明文传给底层的 fetch 库，依然会被截断！
* **对策**：
  * 后端返回和 Vite Mock 接口中，必须对文件名进行百分号编码：`${encodeURIComponent(filename)}`。
  * 前端 `App.vue` 中专门增加了一层 `safeDrawingUrl` 计算属性：提取 uploads 或 converted 文件夹后的文件名，**先 decode 还原为中文原名，再重新 encodeURIComponent 编码文件名**。这保证了网络 fetch 时 `#` 变为 `%23` 避开了截断，同时在磁盘上依然可以维持中文明文的可视原名！

### ⚠️ 避雷针 B：底层渲染核心对 `BASE_URL` 拼接机制的“错层设计”
* **痛点**：底层 CAD 渲染核心在遇到缺字时会去 fetch 字体元数据 `fonts.json` 及其对应的 `.shx` 字体包。**渲染器内部会自动追加 `/fonts/` 子级路径**！
  * 例如当您传入 `BASE_URL` 时，渲染核心实际发出的网络请求是：`${BASE_URL}fonts/fonts.json` 及 `${BASE_URL}fonts/hztxt.shx`。
* **对策**：
  * **千万不能**将 `App.vue` 里的 `BASE_URL` 设为 `'./drawings/fonts/'`（这会导致双写拼接出 `./drawings/fonts/fonts/...` 从而 404 导致文字全白）。
  * **必须将 `BASE_URL` 设为 `'./drawings/'`**。渲染核心在其后拼上 `fonts/...` 后，刚好完美指向 Express 托管的 `release/public/drawings/fonts/...`。

---

## 🔮 5. 下一步计划是什么？ (Next Steps)
如果您要继续扩展或接手本项目，可以考虑执行以下任务：
1. **中文缺字自动映射验证**：
   * 目前我们的 `AcApFontUtil.ts` 已经实现了：当图纸引用的字体不在本地时，自动重定向到本地存在的 `hztxt` 中文字体。如果您发现了特定生僻字体的图纸仍有文字空白，可在 `release/public/drawings/fonts/fonts.json` 中配置该字体的别名，将其 `name` 数组中添加对应的映射名，或者直接让用户将该 `.shx` 拖入 `fonts/` 文件夹，无需重新编译，即可热插拔生效。
2. **移动端手势与视口优化**：
   * 目前系统在 iPad 和手机浏览器上扫码查看大图纸时，双指缩放及拖拽的流畅度以及操作舒适性还有进一步提升的空间，可以在底层 `three-renderer` 或 `cad-simple-viewer` 的视口矩阵控制中微调。
3. **二维码展示的 UI 美化**：
   * 目前前端的 Element Plus 分享 Dialog 比较干净，可以配合用户的意见进一步定制二维码的显示布局、打印排版或者增加导出 PDF 标签卡的功能。

---
*交接完毕！请下一个 AI 助手继续严格遵循 Karpathy 代码规范，保持简单与精准修改！*
