# 🤝 CAD Viewer 项目交接文档 (HANDOFF.md)

本文件是为**新开辟的 AI 助手会话**或**接替的开发人员**编写的完整技术上下文交接。在接手后，通过阅读此文档即可无缝理解项目现状，迅速进入开发状态。

---

## 🎯 1. 我们在做什么任务？ (Project Goal)
本项目目标是基于 `pnpm monorepo` 架构的 **mlightcad/cad-viewer** 建立一个**完全适用于纯内网、物理隔离环境的极速 CAD 全栈查看器与活码分享系统**。
核心需求为：
1. **纯本地决策查看与上传分享双轨交互**：用户拖入 DWG/DXF 图纸后，自主选择：
   * **直接打开**：免流量纯本地极速解析（无需上传后端，纯前端 Canvas + Three.js 绘制）。
   * **上传并生成二维码**：上传至服务器，自动在下方生成一个二维码用于局域网内其他设备扫码查看。
2. **二维码自动保存与加水印**：前端在生成分享二维码的瞬间，全自动合成“带有文件名文字”的水印二维码并下载，同时**静默备份**发送给后端，保存于服务器的 `public/drawings/qrcodes/` 目录中。
3. **全套字体的本地离线自托管**：在没有互联网的内网环境下，所有中英文字体全部拉取本地物理字库，并设计**完美中文缺字兜底机制**，解决图纸文字空白或问号问题。
4. **零依赖极简单文件部署**：前端代码打包并剔除大文件，后端利用 `esbuild` 打包成单个 `server.cjs`（约 1.1MB），只需 Node.js 环境即可一键启动服务。

---

## ✅ 2. 已经完成了什么？ (Accomplishments)

### 2.1 基础架构与交互逻辑
1. **交互逻辑全部重构并回退至纯净版**：
   * 彻底剥离了早期引入 of ODAFileConverter 后端依赖（回退到轻量级的前端 DXF 解析）。
   * 前端实现“选图纸 ➡️ 弹出决策按钮（直接查看/上传分享）”的清晰逻辑。
2. **离线常用字体抓取与就绪**：
   * 通过 Node 离线下载工具抓取了包括 `hztxt.shx`（经典中文兜底）、`txt.shx`、`gbenor.shx` 等 8 款最核心中英文字体，物理存放在开发目录 `packages/cad-viewer-qrcode/public/drawings/fonts/` 下。
3. **完成了 100% 绝对写保护的部署打包管道**：
   * 修正并优化了 `package.json` 中的 `build:release` 编译脚本。日常升级编译时，会自动隔离 `public/` 物理资产盘，**绝对不会重写或覆盖内网服务器上已存在的图纸、二维码和海量字体资产**。

### 2.2 本阶段增量重构与修复成果 (核心更新)
1. **重命名宿主演示项目为 `cad-viewer-qrcode`**：
   * 将 `packages/cad-viewer-example` 彻底重命名为 `packages/cad-viewer-qrcode`。
   * 包名对齐为 `@mlightcad/cad-viewer-qrcode`，并在整个 Monorepo（包含根目录、子包、CI.yml 工作流、copyDist.js 等）全局同步更新了所有的依赖与引用。
2. **动态局域网 IP 获取与智能过滤**：
   * **动态化**：将 IP 的解析由原先的“服务启动时静态获取一次”重构为了**每次客户端请求 `/api/server-info` 时动态实时获取**。彻底解决了电脑中途切换网络/Wi-Fi 时，旧二维码 IP 缓存过期失效的问题。
   * **屏蔽虚拟网卡**：在 `server.js` 中加入了多网卡智能过滤逻辑，主动忽略 `vbox`、`vmnet`、`docker`、`vpn`、`utun` 等虚拟网卡与 VPN 接口干扰，**优先且只返回手机能连通的真实物理局域网 IP**。
3. **老二维码完全兼容与 `+` 号清洗还原**：
   * 针对老二维码中由于 `URLSearchParams` 多重转码导致空格变成 `+` 并最终编码为 `%2B` 引发 404 报错的缺陷，我们在前端 `safeDrawingUrl` 的相对路径与绝对路径解析模块中均增加了 **`.replace(/\+/g, ' ')` 智能清洗**。
   * 现在，手机端扫以前的历史二维码，新系统可以自动将参数中的 `+` 还原为空格并向服务器正常拉取，**老二维码无需重新生成即可正常工作！**
4. **控制台时间戳日志**：
   * 为后端生产服务（Express）的控制台关键输出（HTTP 访问、文件上传、二维码备份）统一加上了本地时间戳前缀，格式为：`[YYYY-MM-DD HH:mm:ss]`。

---

## 📍 3. 当前进展到哪？ (Current Status)
* **最新完美版代码已全部构建成功，且源码与文档已被自动提交并推送至 GitHub 个人仓库的 `QRcode` 分支**。
* 已经在局域网的 Windows/Mac 上通过了全流程验证，老二维码解析兼容正常，IP 动态获取机制运作极其顺畅。
* **本地 Node 生产服务（8080 端口）已在后台开启运行**，工作目录（Cwd）已正确设为 `release/`，访问一切正常。

---

## 💡 4. 核心踩坑与技术避雷针 (Lessons Learned & Crucial Pitfalls)
在续写或修改本项目的代码时，**必须死死记住以下三项极其关键的底层运行机制**：

### ⚠️ 避雷针 A：URL 哈希锚点截断与二次 URL 编码机制
* **痛点**：当上传的图纸文件名包含 **`#`**、空格、`&` 等在 URL 中具有特殊语义的字符（例如 `104 送水泵站5#~7#控制图.dwg`）时，**浏览器在发送 HTTP 物理请求时，会在 `#` 的位置直接截断**，从而发生 404 报错。
* **二次解码机制**：当前端从浏览器地址栏通过 `urlParams.get('drawing')` 提取参数时，浏览器会**自动执行一次 URL 解码**，将安全的 `%23` 字符重新变回明文的 `#`。如果不加干预直接传给底层，网络请求依然会被截断！
* **对策**：前端 `App.vue` 中设计了 `safeDrawingUrl` 计算属性：提取相对/绝对路径文件名后，先 decode 还原为中文原名（并过滤掉误转的 `+` 号），再对每一段文件名运行 `encodeURIComponent`，确保网络 fetch 时 `#` 变为 `%23`、空格变为 `%20`。这保证了网络 fetch 时 `#` 变为 `%23`，同时在磁盘上依然可以维持中文明文的可视原名！

### ⚠️ 避雷针 B：底层渲染核心对 `BASE_URL` 拼接机制的“错层设计”
* **痛点**：底层 CAD 渲染核心在遇到缺字时会去 fetch 字体文件，**渲染器内部会自动在传入的 `BASE_URL` 后面追加 `/fonts/` 子级路径**！
  * 例如实际发出的网络请求是：`${BASE_URL}fonts/fonts.json`。
* **对策**：**千万不能**将前端的 `BASE_URL` 设为 `'./drawings/fonts/'`（这会导致双写拼接出 `./drawings/fonts/fonts/...` 从而 404 导致文字全白）。**必须将 `BASE_URL` 设为 `'./drawings/'`**，让渲染核心拼上 `fonts/...` 后刚好能正确指向 Express 托管的数据目录。

### ⚠️ 避雷针 C：后端生产服务的运行时当前工作目录 (Cwd) 限制
* **痛点**：后端服务为了高移植性，使用 `process.cwd()`（当前终端工作目录）来定位静态资源目录 `dist` 和物理数据盘 `public`。
* **对策**：启动服务时，**终端的当前工作路径必须先切换到 `server.cjs` 所在的部署目录下**（即 `release/` 目录，如：`cd release && node server.cjs`）。如果在项目根目录或其他无关路径下直接以绝对路径执行服务，会导致 Express 找不到同级的网页 dist 目录，导致网站直接报 **404 Not Found**。

---

## 🔮 5. 下一步计划是什么？ (Next Steps)
如果您要继续扩展或接管本项目，可以考虑执行以下任务：
1. **配置静态 IP 或局域网 DNS**：
   * 由于内网中服务器的局域网 IP 改变会导致老二维码（里面写死了旧 IP 链接）失效，在正式内网部署时，强烈建议为该服务器电脑配置固定静态 IP，或者在内网配置统一的局域网域名（如 `http://cad-viewer.local`）以保证二维码永久有效。
2. **移动端手势与视口优化**：
   * 目前在 iPad 和手机浏览器上扫码查看大图纸时，双指缩放及拖拽的流畅度还有提升空间，可以在底层 `three-renderer` 或 `cad-simple-viewer` 的视口矩阵控制中进一步微调。
3. **中文缺字自动映射验证**：
   * 目前 `AcApFontUtil.ts` 已经实现了当图纸引用的字体不在本地时，自动重定向到本地存在的 `hztxt` 中文字体。如果未来发现了特定图纸显示问号，可以直接将该 `.shx` 字体文件丢入服务器上的 `release/public/drawings/fonts/` 目录下，网页刷新后即可热插拔自动加载生效。

---
*交接完毕！请下一个 AI 助手继续严格遵循 Karpathy 代码规范，保持简单与精准修改！*
